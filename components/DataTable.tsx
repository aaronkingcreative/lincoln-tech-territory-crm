export function DataTable({ rows }: { rows: Record<string, unknown>[] }) { const cols = Array.from(new Set(rows.flatMap(r=>Object.keys(r)))).slice(0,12); return <div className="overflow-auto rounded bg-white shadow"><table className="min-w-full text-sm"><thead><tr>{cols.map(c=><th className="border-b p-2 text-left" key={c}>{c}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={String(r.id ?? i)}>{cols.map(c=><td className="border-b p-2 align-top" key={c}>{String(r[c] ?? '')}</td>)}</tr>)}</tbody></table></div> }
